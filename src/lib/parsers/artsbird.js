/**
 * 中国上海国际艺术节 (China Shanghai International Arts Festival)
 * URL: https://www.artsbird.com/
 *
 * NOTE: This website is a Nuxt.js SPA (Single Page Application).
 * The saved HTML contains only a <div id="__nuxt"> shell with CSS/JS,
 * and no server-rendered event content. All content is rendered
 * client-side via JavaScript, which cannot be extracted from static HTML.
 *
 * A headless browser or dynamic rendering approach would be needed
 * to extract events from this site.
 */
export function parseArtsbirdEvents(_html, _source) {
  return [];
}
